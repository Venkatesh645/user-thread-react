const initialState = {
  user: null,
  pending: false,
}
const registerLogin = (state = initialState, action) => {
  switch(action.type){
    default:
    return state;
  }
}
export default registerLogin